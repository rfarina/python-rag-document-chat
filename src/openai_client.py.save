# import openai
# from src.config import OPENAI_API_KEY
# from openai import OpenAI

# # Initialize OpenAI API Key
# openai.api_key = OPENAI_API_KEY

# def generate_response(prompt: str, context: str, model: str = "gpt-4o") -> str:
#     """
#     Generate a response from OpenAI Chat API using a given prompt and context.
    
#     Args:
#         prompt (str): User's input or query.
#         context (str): Relevant context retrieved from the vector store.
#         model (str): OpenAI model to use (default: gpt-4o).
    
#     Returns:
#         str: The assistant's response.
#     """
#     try:
#         print("H E L L O\n")
#         print(f"Generating response for prompt:\n {prompt}")
#         # Construct the input message with system instructions and context
#         messages = [
#             {"role": "system", "content": "You are a helpful assistant. Use the provided context to answer the user's query accurately."},
#             {"role": "user", "content": f"Context: {context}\n\nQuery: {prompt}"}
#         ]

#         client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

#         completion = client.chat.completions.create(
#             model="gpt-4",
#             messages=[
#                 {"role": "system", "content": "You are a helpful assistant."},
#                 {"role": "user", "content": prompt}
#             ]
#         )


#         # Extract the content from the response
#         return completion.choices[0].message
    
#     except Exception as e:
#         # return f"Error generating response: {e}"


# def generate_embedding(text: str, model: str = "text-embedding-ada-002") -> list:
#     """
#     Generate an embedding for the given text using OpenAI's Embedding API.

#     Args:
#         text (str): The input text to embed.
#         model (str): OpenAI embedding model (default: text-embedding-ada-002).

#     Returns:
#         list: Embedding vector for the input text.
#     """
#     try:
#         # Call the OpenAI Embedding API
#         response = openai.Embedding.create(
#             input=text,
#             model="chat-gpt-4"
#         )

#         # Return the embedding vector
#         return response["data"][0]["embedding"]
    
#     except Exception as e:
#         raise ValueError(f"Error generating embedding: {e}")
